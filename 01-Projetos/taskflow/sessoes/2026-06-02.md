---
type: sessão
status: concluído
projeto: crm-saas
tags: [sessão, sprint-1, sprint-2, sprint-3, landing-page, gsap]
created: 2026-06-02
updated: 2026-06-02
---

# Sessão 2026-06-02 — Sprints 1-3 + Landing Page

## Commits do dia
- `c27cdca` — feat: sprint 1-3 segurança, qualidade e features
- `aa6e5d7` — feat: landing page scrollytelling GSAP + Lenis

---

## Sprint 1 ✅ (C5 + C7 feitos hoje)

**C5 — finance.service.ts:** `throw new Error` → `throw new NotFoundException` em deleteInstallment, deleteEntry, updateEntry.

**C7 — Audit logs nos remove:**
- vault.service: `remove(id, tenantId, userId)` → grava `VAULT_DELETE`
- devspace.service: `remove(id, tenantId, userId)` → grava `DEVSPACE_DELETE`
- creatives.service: `remove(id, tenantId, userId)` → grava `CREATIVE_DELETE`
- Controllers atualizados para passar userId. vault.service.spec.ts atualizado.

Itens já prontos verificados: C1, C2, C3, C4, C6, C8.

---

## Sprint 2 ✅ (I3, I5, I6, I8, I11 feitos hoje)

**I3:** Removido `any` em tasks.service e reports.service. Criado tipo `ProjectWithReport = Prisma.ProjectGetPayload`.

**I5:** Criado `error-boundary.tsx` aplicado no layout do dashboard.

**I6:** `confirm()` nativo substituído por confirmação dupla inline com timeout 4s em `lead-drawer.tsx`.

**I8 — Sentry:** `sentry.client.config.ts` + `sentry.server.config.ts` (web) e `instrument.ts` (api). DSN: configurado nas env vars Vercel/Railway pelo usuário.

**I11 — TaskDetailSheet:** drawer lateral com SubtasksPanel (add/toggle/rename/delete) e CommentsPanel (CRUD, só autor edita/deleta). Clique no card kanban → detail sheet. Botão lápis → edit modal.

Itens já prontos verificados: I1, I2, I7, I9, I10.

---

## Sprint 3 ✅

**D6 — Dark mode:** já estava implementado (ThemeProvider + toggle na sidebar).

**D8 — CSV export:** botão "Exportar" na header do /finance. Exporta aba ativa (Lançamentos ou Parcelas) com BOM UTF-8 para Excel.

**D5 — Kanban mobile:** tabs horizontais por coluna em telas <768px via matchMedia. Coluna ativa ocupa 100% da largura.

---

## Landing Page Scrollytelling ✅

Instaladas: `gsap`, `lenis`.

Componentes criados em `apps/web/src/components/landing/`:
- `LandingWrapper.tsx` — Lenis + GSAP ticker, cleanup, prefers-reduced-motion
- `NavbarNew.tsx` — frosted glass ao rolar 80px
- `shared/ScrollProgress.tsx` — barra teal 2px no topo
- `shared/CustomCursor.tsx` — cursor teal 20px com lag (desktop only)
- `sections/HeroNew.tsx` — 300vh pinned, scrub timeline
- `sections/ProblemNew.tsx` — 200vh pinned, cards esq/dir
- `sections/SolutionNew.tsx` — 200vh pinned, bg transition
- `sections/FeaturePortalNew.tsx` — 250vh pinned, clip-path reveal
- `sections/FeatureCRMNew.tsx` — 250vh pinned, lead cards caindo
- `sections/FeatureAINew.tsx` — 200vh pinned, typewriter por scroll
- `sections/MetricsNew.tsx` — countUp (8h, 40%, 3x, 100%)
- `sections/TestimonialsNew.tsx` — carousel GSAP autoplay
- `sections/PricingNew.tsx` — stagger reveal R$197/497/997
- `sections/CTAFinalNew.tsx` — headline explode + glow pulse

Regras aplicadas: só transform/opacity, prefers-reduced-motion, mobile sem pin, will-change localizado, zero TS errors.

---

## Pendências
- [ ] Erro de deploy Vercel — sendo resolvido
- [ ] Confirmar Sentry no Railway (SENTRY_DSN)
- [ ] Testar landing 375px + 1440px
